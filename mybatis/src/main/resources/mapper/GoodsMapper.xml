<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <mapper namespace="ksmart.mybatis.mapper.GoodsMapper"> <!-- 경로위치 -->
 
 	<resultMap type="Goods" id="GoodsResultMap">
 		<id 	column="g_code" 		property="goodsCode"/> <!-- id -> PK -->
 		<result column="g_name" 		property="goodsName"/> <!-- result -> 일반컬럼, FK -->
 		<result column="g_price" 		property="goodsPrice"/>
 		<result column="g_seller_id" 	property="goodsSellerId"/>
 		<result column="g_reg_date" 	property="goodsRegDate"/>					
	</resultMap>
 	
    <insert id="addGoods" parameterType="Goods">   
      INSERT INTO tb_member
      (   g_code
         ,g_name
         ,g_price
         ,g_seller_id
         ,g_reg_date
      )VALUES(
         #{goodsCode}
         ,#{goodsName}
         ,#{goodsPrice}
         ,#{goodsSellerId}
         ,CURDATE()
      );
     </insert>
     
  
     <!-- property에 mybatis.type-aliases-package=ksmart.mybatis.dto 입력함 -->
     <select id="getGoodsList" resultMap="GoodsResultMap"> 
        /* 상품목록 조회 */
        SELECT
         g.g_code
         ,g.g_name
         ,g.g_price
         ,g.g_seller_id 
         ,g.g_reg_date
      FROM
         tb_goods AS g;
     </select>
  </mapper>
 